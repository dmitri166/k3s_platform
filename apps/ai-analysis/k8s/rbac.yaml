---
# ServiceAccount used by the CronJob pods
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ai-analysis
  namespace: ai-analysis
  labels:
    app.kubernetes.io/name: ai-analysis
    app.kubernetes.io/part-of: k3s-platform

---
# ClusterRole: read-only view of cluster resources needed for context enrichment
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: ai-analysis-reader
  labels:
    app.kubernetes.io/name: ai-analysis
    app.kubernetes.io/part-of: k3s-platform
rules:
  # Pod, Node, Event data
  - apiGroups: [""]
    resources: [pods, nodes, events, namespaces, persistentvolumeclaims]
    verbs: [get, list, watch]
  # Deployment / ReplicaSet health
  - apiGroups: [apps]
    resources: [deployments, replicasets, statefulsets, daemonsets]
    verbs: [get, list]
  # CronJob itself (for self-inspection)
  - apiGroups: [batch]
    resources: [cronjobs, jobs]
    verbs: [get, list]

---
# Bind the ClusterRole to the ServiceAccount
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: ai-analysis-reader
  labels:
    app.kubernetes.io/name: ai-analysis
    app.kubernetes.io/part-of: k3s-platform
subjects:
  - kind: ServiceAccount
    name: ai-analysis
    namespace: ai-analysis
roleRef:
  kind: ClusterRole
  name: ai-analysis-reader
  apiGroup: rbac.authorization.k8s.io
