apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ai-analysis-reports
  namespace: ai-analysis
  labels:
    app.kubernetes.io/name: ai-analysis
    app.kubernetes.io/part-of: k3s-platform
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # Uses the default K3s local-path storage class.
  # Change to a network-backed StorageClass if you need ReadWriteMany.
  storageClassName: local-path
